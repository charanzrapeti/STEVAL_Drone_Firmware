#include "spi.h"

void spi_init(void)
{
    /* Placeholder for the SPI driver implementation. */
}

void spi_write_byte(uint8_t data)
{
    (void)data;
}

void spi_write_block(const uint8_t *data, uint32_t length)
{
    (void)data;
    (void)length;
}
